# Dependencies
node_modules/
.pnpm-store/

# Environment variables (NEVER commit these)
.env
.env.local
.env.*.local
.env.*.txt

# Build and output
.next/
out/
dist/
build/
.vercel
.builder-log.md

# Package managers
pnpm-lock.yaml
package-lock.json
yarn.lock
yarn-error.log
yarn-debug.log

# IDE and editor
.vscode/
.idea/
*.swp
*.swo
*~
*.sublime-project
*.sublime-workspace

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# SSL/TLS
*.pem

# Database and config (keep schema files tracked for collaboration)
# schema.sql - UNCOMMENT if you want to track it
# schema.stripe.sql - UNCOMMENT if you want to track it

# TypeScript incremental build cache (generated during compilation)
tsconfig.tsbuildinfo

# Chrome Extension: Ignore generated manifest (environment-specific), keep templates
public/chrome-extension/manifest.json
!public/chrome-extension/manifest.*.json

# Test/script outputs (keep test files, exclude outputs)
*.test.txt
test-output/

# Keep everything else (documentation, config files)
# Remember: ALL source files should be tracked

# Include the entire docs folder in the repository
docs/
docs/**
*.md 
!README.md
*.txt
*.sql
*.sh
.npmrc
todo.md